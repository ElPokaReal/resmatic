# ResMatic Frontend example environment variables
# Copy this file to .env.local (preferred) or .env before running the app.
# NOTE: Files named .env* are gitignored in this repo. Keep secrets out of version control.

# Base URL of the backend API used by the frontend
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001/api/v1

# Port for Next.js dev/start server
PORT=3000

# Optional: disable Next.js telemetry locally
NEXT_TELEMETRY_DISABLED=1
